function sumNumbers(a,b){
    return a + b 
}
let final = sumNumbers(2,5);
console.log(final + 2);  // undefine

function sum (a,b) {
    let x = a + b ;
    console.log(x);
    return x;
}
let total = sum(2, 3);
console.log(total * 2);

// NOte : 
// 1. Use return to pass the value
// 2. U cannot write anything after return
