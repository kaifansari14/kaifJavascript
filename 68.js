// try ...catch...finally
// It's a way to handel error in javaScript so your progeram doesn't crash.

// Synatax 

try {
    // code that might cause an error
} catch (error) {
    // Code that runs *if* an error happens
} finally{
    // Code that always runs (error or not)
}

